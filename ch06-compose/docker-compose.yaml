version: "3.8"
services:
  mongodb:
    image: 'mongo'
    volumes:
      - data:/data/db
    environment:
      # # 寫法一
      # - MONGO_INITDB_ROOT_USERNAME=max
      # - MONGO_INITDB_ROOT_PASSWORD=secret
      # # 寫法二
      # MONGO_INITDB_ROOT_USERNAME: max
      # MONGO_INITDB_ROOT_PASSWORD: secret
    # 寫法三：使用 .env 檔案
    env_file: ./env/mongodb.env
    # networks: # 可以不用寫，compose 會預設
    #   - goals-net
  # backend:
  #   image:
  # frontend:
  #   image:

volumes: # 有用到命名卷，要另外在這裡設定
  data: # 冒號後沒有值